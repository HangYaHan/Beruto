Design a QT UI desktop canvas with a command-center feel for multi-asset backtesting.

角色与场景：目标用户是拥有轻度金融知识的投资者（他们有其他工作，使用本项目工具来验证自己的投资策略的收益）；典型使用场景（策略回测、）？
-- 对于策略回测：需要支持基本的单一项目回测，同时也要支持类似网格策略回测（通过枚举暴力求解特定策略组合在指定标的下的最优/较优超参组合）和机器学习回测（通过机器学习求解特定策略组合在指定标的下的最优/较优超参组合）
本地化与语言：界面语言为纯英文。
任务流：用户的主要流程是“配置参数→提交回测→查看进度→分析结果”
AI/助手：是否需要内置“命令输入/自然语言配置策略”或“解释结果”的助手模块？
无障碍与性能：不需要无障碍功能；不需要渲染功能，只需绘制简单的图形与线条即可。
设备与分辨率：面向WINDOWS桌面应用。
资产与市场：仅需支持A股即可；
主题与品牌：先使用默认的颜色，美化工作先暂时不考虑。
操作密度：我希望界面更密集（信息墙），像是核电站/航空中心console控制台那样的感觉
信息优先级：主屏最想突出哪三项指标/视图。主屏最核心的一定是K线图，同时把买卖点的行为标注在图上。由于会涉及到多只股票/多个标的，可以设置选项卡进行切换；其次我可以在图上选择任意一点，查看此时的持仓情况，以及各种数据指标，这些数据可以在旁边的面板上查看。次要核心的是命令行窗口，命令行作为该程序当前的唯一输入手段，同时也要兼具输出进度信息，提示文本的功能。

标签：是否需要一个固定的“全部任务”标签，展示整体队列状态，还是只按标的分标签？需要这样一个全部任务标签
K 线交互：需要联动十字线 + 悬浮提示 + 滚轮缩放吗？需要。默认要叠加哪些指标（如 MA、布林带）？默认叠加MA-5;MA-20;
买卖标记：颜色/形状偏好吗（如绿色上三角=买，红色下三角=卖）。可以；悬浮时是否展示成交量、价格、单笔盈亏？需要
右侧面板字段：点击任意时间点时，必须显示哪些字段（仓位数量、均价、浮盈/浮亏、已实现盈亏、现金、敞口%、触发信号名、参数组）？是的，都需要 
网格/ML 回测：是否需要一个任务队列条，显示并行任务进度、ETA，放在控制台旁边还是右栏内？放在右栏内。
命令行：单行输入 + 历史，还是输入区+滚动日志分离？需要快捷键（如 Ctrl+L 清屏、↑ 查看历史）吗？不需要快捷键；输入区+滚动日志分离。
任务状态：要展示三种状态（运行、完成、失败），是否需要“重试/取消”按钮？需要
主题：暂用深色（炭黑背景+灰阶+青色点缀）可以吗？可以