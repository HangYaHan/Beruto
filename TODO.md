# TODO (Step by Step)

> 原则：始终保持“可运行 + 可验证”，宁可多拆步骤，不做跨越式开发。

## Phase 0: 基线确认
- [ ] Step 0.1 执行 `cargo run`，确认主程序在本机可正常运行。
- [ ] Step 0.2 执行 `cargo test`，记录当前测试通过情况。
- [ ] Step 0.3 检查 `data/159581_daily.csv` 是否存在且非空。
- [ ] Step 0.4 在 README 中补一条“当前基线结果时间戳”。

## Phase 1: 数据层最小增强（不改策略逻辑）
- [ ] Step 1.1 在 `DailyQuote` 增加最小校验函数（如价格必须大于 0）。
- [ ] Step 1.2 在 CSV 读取后过滤/报错异常行，并输出统计信息。
- [ ] Step 1.3 给数据加载增加单元测试：空文件、缺列、非法数字。
- [ ] Step 1.4 在 `main` 启动时打印“有效行数/无效行数”。

## Phase 2: 回测引擎最小增强（保持当前接口）
- [ ] Step 2.1 在回测结果中增加 `start_date` 和 `end_date`。
- [ ] Step 2.2 在回测结果中增加 `holding_days`（有仓位的天数）。
- [ ] Step 2.3 增加年化收益率计算（先用简单交易日近似）。
- [ ] Step 2.4 给最大回撤函数补充边界测试（空曲线、单点曲线、单调上涨/下跌）。

## Phase 3: 策略参数实验（先做 CLI）
- [ ] Step 3.1 把 sweep 参数提取为常量，避免写死在 `main`。
- [ ] Step 3.2 新增命令行参数：`--buy-drop`、`--sell-rise`（可选，提供默认值）。
- [ ] Step 3.3 新增命令行参数：`--initial-capital`。
- [ ] Step 3.4 当参数缺失或非法时，输出友好错误信息和示例。
- [ ] Step 3.5 补充参数解析测试（合法/非法输入）。

## Phase 4: 交易成本与滑点（最小版本）
- [ ] Step 4.1 在引擎增加固定手续费参数（先按每次成交固定比例）。
- [ ] Step 4.2 在引擎增加固定滑点参数（按买高卖低处理）。
- [ ] Step 4.3 在结果里增加 `total_fees` 与 `total_slippage`。
- [ ] Step 4.4 增加对比测试：有成本时收益应不高于无成本。

## Phase 5: 存储层落地（填充 `src/data/storage.rs`）
- [ ] Step 5.1 定义结果导出结构（CSV 字段列表先固定）。
- [ ] Step 5.2 导出净值曲线到 `output/equity_curve.csv`。
- [ ] Step 5.3 导出汇总指标到 `output/summary.json`。
- [ ] Step 5.4 增加导出文件存在性和字段完整性测试。

## Phase 6: 策略可解释性输出
- [ ] Step 6.1 记录每次买卖触发原因（跌幅/涨幅达到阈值）。
- [ ] Step 6.2 增加交易日志导出 `output/trades.csv`。
- [ ] Step 6.3 在 CLI 输出最近 N 次交易摘要。
- [ ] Step 6.4 增加“无交易发生”场景的输出处理。

## Phase 7: GUI 最小起步（只读展示，不做交互策略编辑）
- [ ] Step 7.1 选定 GUI 技术栈并做最小窗口启动样例。
- [ ] Step 7.2 在窗口展示回测摘要文本（总收益/回撤/交易次数）。
- [ ] Step 7.3 在窗口展示净值曲线（先静态绘制）。
- [ ] Step 7.4 增加“GUI 启动失败时自动回退 CLI”的处理。

## Phase 8: 质量与工程化
- [ ] Step 8.1 增加 `cargo fmt` 与 `cargo clippy` 的检查步骤。
- [ ] Step 8.2 统一错误类型，减少 `Box<dyn Error>` 的外溢范围。
- [ ] Step 8.3 为关键模块补文档注释（引擎、策略接口、数据格式）。
- [ ] Step 8.4 在 README 增加“常见问题与排错”小节。

## Phase 9: 扩展准备（保持小步）
- [ ] Step 9.1 抽象数据源接口，为后续在线数据接入预留点位。
- [ ] Step 9.2 新增第二个简单策略（如均线交叉）用于横向对比。
- [ ] Step 9.3 增加策略对比报告输出（同一数据、同一初始资金）。
- [ ] Step 9.4 评估是否需要引入并行回测（只做设计，不急着实现）。
